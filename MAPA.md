# MAPA — core-api (backend)

> **Drogowskaz aplikacji.** Tu jest zapisane CO i GDZIE jest. Każdy nowy element
> (endpoint, serwis, model, integracja) **musi** zostać tu dopisany, a każda
> istotna zmiana — zaktualizowana. To jedyne źródło prawdy o strukturze.
>
> **ZASADA UTRZYMANIA:** dodajesz/zmieniasz kod → w tym samym commicie aktualizujesz MAPĘ.
> Mapa frontu: `core-crm-frontend/MAPA.md`.

---

## 1. Czym jest aplikacja

CRM + automatyzacja księgowo-logistyczna dla **Surf Stick Bell** (kosmetyki/surf
sticki). Obsługuje dwa rynki:
- **PL** — faktury przez **iFirma**, KSeF, JPK.
- **Kanary/ES** — faktury przez **Contasimple** (IGIC 7%), WZ (albaran).

Plus: wysyłki kurierskie **GlobKurier**, odbiór i klasyfikacja maili (IMAP),
agenci AI (Anthropic) sterowani z **Telegrama** (master w n8n) i z panelu CRM.

Stack: Node/Express + Prisma/Postgres. Deploy: `npx prisma db push && node src/index.js`
(nowe modele/pola wchodzą same). Auth: nagłówek `x-api-key` (`src/index.js` → `auth`).

**Obraz produkcyjny definiuje `Dockerfile`** (node:22-bookworm-slim + apt:
poppler-utils, imagemagick, ghostscript, tesseract-ocr, openssl dla silników
Prisma). Powód: sam `aptPkgs` w `nixpacks.toml` NIE dowoził binariów do obrazu
runtime — na produkcji leciało `spawn pdfinfo ENOENT`, choć poppler-utils był
w configu od dawna. `nixpacks.toml` został wyłącznie jako zapas (gdyby builder
był przypięty do Nixpacks) i podaje pakiety dwoma mechanizmami naraz: `aptPkgs`
(nazwy Debiana, z myślnikiem) i `nixPkgs` (nazwy nixpkgs, z podkreślnikiem).
**Weryfikacja binariów tylko w runtime:** `GET /api/_version` → pole `binaria`
(realne uruchomienie procesu, nie odczyt configu) + głośne ostrzeżenie w logu
przy starcie — `services/bin-check.js`.

---

## 2. Punkt wejścia i montaż tras

`src/index.js` — bootstrap, middleware auth (`/api`), montaż tras, start `inbox-poller`.

| Prefix URL | Plik | Obszar |
|---|---|---|
| `/` | `routes/map.js` | health/landing |
| `/img` | `express.static(public/img)` | **Statyczne obrazki stopki mailowej** (logo, ikony social). Montowane jako PIERWSZE w `src/index.js` — przed autoryzacją i przed globalnym `express.json`, bo pobierają je klienty pocztowe (Gmail, Outlook) anonimowo, bez nagłówków; 401 = pusty obrazek w stopce u odbiorcy. Nagłówki: `Cache-Control: public, max-age=2592000, immutable` (30 dni). **Zmiana grafiki = nowa nazwa pliku**, nie podmiana treści pod tym samym adresem (Gmail cache'uje przez proxy googleusercontent). `_sonda.png` (1×1) zostaje na stałe jako probe montażu: `curl -sI /img/_sonda.png` |
| `/` | `routes/preprocess-scan.js` | **POST /preprocess-scan** (n8n, przed OCR): cyfrowy PDF → `{skip:true}`; skan/obraz → strony PNG 300 dpi z korektą obrotu (tesseract OSD) + deskew/normalize → `{pages:[base64…]}`. Auth: `x-token` = `PREPROCESS_TOKEN` (brak env = otwarty), własny limit body 25 MB, POZA /api. Wymaga binariów z obrazu (Dockerfile): poppler-utils, imagemagick, tesseract-ocr |
| `/` | `karta-pracy.js` | **POST /karta-pracy/crop** (n8n): skan KARTY EWIDENCJI CZASU PRACY (PDF base64, 1 karta = 1 strona) → wykrycie ramki i siatki tabeli (profile ciemnych pikseli, deskew) i pocięcie na pasma po 10-11 wierszy: `naglowek`, `lewa[0..2]` (dzień/rozpocz/zakończ/RAZEM), `prawa[0..2]` (dzień + RAZEM…Chor.), **`suma`** (wiersz podsumowania z wierszem 31 nad nim, powiększony do 2200 px — ma inną strukturę niż wiersze dni i doklejony na końcu trzeciego pasma był czytany najgorzej z karty), **`sumaKomorki`** (pięć POJEDYNCZYCH rubryk wiersza SUMA — `razem`/`sto`/`nocne`/`uw`/`chor`, każda wycięta osobno i powiększona 4x lanczosem; model dostaje jedną liczbę na obrazek zamiast szukać jej wśród siedmiu rubryk, a mylenie kolumn było ostatnim poważnym źródłem błędów), `pod` — JPEG base64. **Wysokość wiersza z MEDIANY odstępów** między wykrytymi liniami, nie z (dół−góra)/32 — ostatnia wykryta linia bywa linią stopki, a 2% błędu kumuluje się przez 32 wiersze i zsuwa wycinek ostatniego wiersza o pół rubryki. Kolumna „dzień" jest w KAŻDYM wycinku (zszywanie lewej z prawą po numerze dnia); kolumny podpisów wycięte. `tylkoInfo:true` → sama liczba stron, `pages:[n]` → tnie wybrane. Strona bez pewnej siatki wraca z `blad` (fail-closed, nie idzie do modelu na oślep). Auth: `x-token` = `PREPROCESS_TOKEN` (brak env = otwarty), własny limit body **40 MB** (montowany PRZED globalnym `express.json` 32 MB — inaczej globalny by go przyciął), POZA /api. Wymaga: poppler-utils (pdfinfo/pdftoppm), imagemagick (convert -deskew) + npm `sharp`. CLI: `node src/karta-pracy.js <pdf> ./out`. **renderPage używa JPEG jako formatu pośredniego** (pdftoppm -jpeg → convert -deskew) — szybciej niż PNG, wykrycie siatki identyczne |
| `/` | `karty-pracy-odczyt.js` | **POST /karty-pracy/odczytaj** (n8n): teczka kart w jednym PDF → dla KAŻDEJ strony crop (`karta-pracy.cropCard`) + **odczyt drobnoziarnisty**: nagłówek osobno (1 obraz) i każde z 3 pasm osobno (~11 dni, 2 obrazy) — raz wierszami (A), raz kolumnami (B); długa tabela rozmywała uwagę modelu i gubiła pojedyncze dni albo wiersz SUMA. **Rozjemca** (całą kartę naraz, inna ziarnistość) dochodzi TYLKO gdy A i B się nie domykają. → `zszyj()`: **dedykowany odczyt wiersza SUMA** z wycinka `suma` (pierwszeństwo nad wierszem doklejanym do pasma) + **głosowanie większościowe** (`wiekszosc` — wartość wygrywa przy ≥2 zgodnych głosach; brak większości → `sporne`; głosowaniu podlega też **nazwisko**, a jego brak blokuje kartę) + kontrola arytmetyki. **Podstawą godzin jest suma wpisów DZIENNYCH, nie wiersz SUMA** (na prawdziwych kartach dni były poprawne w 5 na 6 przypadków, a wiersz SUMA mylił się regularnie i bywa po prostu niewypełniony). Wiersz SUMA to KONTROLA: gdy jest, musi zgadzać się z sumą dni, gdy go nie ma — karta przechodzi z `brakWierszaSumy: true` bez problemu. Tak samo kolumny 100%/UW/Chor./nocne: z wiersza SUMA jeśli są, inaczej liczone z dni; rozjazd między źródłami = problem. Dodatkowo 100% nie może przekraczać RAZEM, 100% tylko w święta ustawowe, wielokrotność 0.5, zakres 0-24, a brak jakiegokolwiek odczytanego dnia blokuje kartę + `wymiarCzasuPracy` (art. 130 KP, święta z Wielkanocy). Zwraca ~2 kB JSON na teczkę: `{stron, rok, miesiac, norma, kartOk, problemyOgolne, karty:[{C, G, problemy, sporne}]}` — **obrazy ani surowe `dni` NIE wracają** (n8n zapisywał wejście/wyjście każdego node’a i dławił się 20 MB obrazów). Pula `rownolegle` (domyślnie 4, max 8), `dpi` (domyślnie 300, 150-400), `model` (domyślnie claude-opus-5), **`strony: [1,2]`** (tylko wybrane strony zamiast całej teczki — pozwala n8n wołać porcjami, gdyby proxy ucinało długie żądania; odpowiedź niesie `przetworzone`, a strona spoza zakresu → błąd zamiast cichego pominięcia), **`rok`/`miesiac`** (narzucenie okresu z zewnątrz, np. z tematu maila), **`nazwiska: ["Imię Nazwisko"]`** (zamknięta lista pracowników — model wybiera z niej, a odczytane nazwisko jest walidowane przynależnością do listy; nazwisko spoza listy wstrzymuje kartę. Normalizacja odporna na kolejność, diakrytyki, wielkość liter i Ł/Ż). `max_tokens` **16000** (podniesione z 8000: na opus-5 adaptive thinking liczy się do limitu; odpowiedź niesie licznik **`uciecia`** — ile odpowiedzi modelu zatrzymało się przed `end_turn`, logowane też w konsoli; pomiar hipotezy H4). **Rubryka „Miesiąc/rok" na formularzu zawiera SAM miesiąc** — brakujący rok jest domyślany (`domyslnyRok`: najświeższy rok, w którym ten miesiąc zdążył się skończyć), a karta niesie `rokDomyslny: true`, żeby założenie było widoczne. Bez tego każda karta zgłaszała „nie odczytano miesiaca/roku", a brak roku uniemożliwiał wyliczenie świąt i sypał fałszywą uwagą o godzinach 100%. Gdy odczyt się nie sparsuje, karta dostaje pole **`diagnostyka`** (powód zatrzymania, zużyte tokeny, początek i koniec surowej odpowiedzi) — a przy `stop_reason: max_tokens` jawny problem „odpowiedz ucieta na limicie". Wymaga **ANTHROPIC_API_KEY**; auth i montaż jak /karta-pracy/crop (x-token, własny parser 40 MB, poza /api). CLI: `node src/karty-pracy-odczyt.js <pdf> [rownolegle]` |
| `/` | `../czytnik/src/router.js` | **TYMCZASOWY montaż Czytnika** (docelowo osobny serwis Railway z katalogu `czytnik/`): **GET /czytnik/zdrowie**, **POST /czytnik/odczytaj** (`data` base64 PDF; `async:true` → `202 {przebiegId}` + **GET /czytnik/przebieg/:id**), **GET /czytnik/norma?rok=&miesiac=** (wymiar czasu pracy z art. 130 KP — zgodny z 42 z 43 zakładek arkusza GODZINY), **POST /czytnik/zlecenia** (DRUGI FORMULARZ — „Ewidencjonowanie czasu wykonywania umowy zlecenie": 3 kolumny, godziny jako PRZEDZIAŁ liczony przez kod, brak listy nazwisk i brak wiersza SUMA; `src/zlecenia.js` + `src/prompty-zlecenie.js`; potwierdzeniem jest drugi czytelnik innego dostawcy, bramką zgodność GODZIN a nie nazwiska; schemat zna też `skreslone` (przekreślony wiersz = 0 h; z nietkniętym podpisem idzie do człowieka), `zapis2` (dwie zmiany w jednym dniu sumują się) i `zapisPodpis` (liczba godzin dopisana przy podpisie — na części kart jedyne miejsce z wynikiem) — wszystkie trzy dołożone po ślepej kontroli 38 kart 8/2026, która wykazała 13 rozjazdów, w tym 4 na kartach z etykietą `auto`; zwraca gotową tabelę `osoby` dla kadr), **POST /czytnik/nowa-zakladka** (buduje zakładkę następnego miesiąca: TOTAL→POPRZEDNI OKRES, NOCNE TOTAL→NOCNE POPRZEDNI, norma i formuły; blokuje, gdy zamykany miesiąc niekompletny), **POST /czytnik/karty-do-druku** + **GET /czytnik/karty-do-druku.zip** (komplet: jeden PDF na miesiąc) + **GET /czytnik/karty-do-druku.pdf** (pojedynczy plik) — PUSTE karty ewidencji na kilka miesięcy do przodu dla całej listy: `src/karta-druk.js` rysuje wzór kadrowy z `assets/karta-wzor.json` (ekstrakt z pliku Ali) i wpisuje miesiąc, nazwisko i wymiar godzin z art. 130 KP; dział zostaje pusty, tak jak na kartach papierowych. Pakowanie ZIP bez zależności: `src/zip.js`, **GET /czytnik/pracownicy** (lista + działy), **POST /czytnik/drugi-odczyt** (pomiarowa; sam drugi czytelnik jest już WPIĘTY w /czytnik/odczytaj jako ścieżka dowodowa E — 98,9% zgodności na 462 polach sierpnia, 0,175 USD/miesiąc: ten sam prompt i schemat przez OpenAI — `src/silnik-openai.js`, domyślnie gpt-5.6-luna, wymaga `OPENAI_API_KEY`; drugi czytelnik innego dostawcy myli się w innych miejscach niż powtórka tym samym modelem), **POST /czytnik/ocr-proba** (POMIAROWA, hipoteza ZMIERZONA I ODRZUCONA — na piśmie odręcznym Vision ma 68% zgodności i 0 trafień w klasie „przeoczony wpis", więc NIE jest wpięty w odczyt: ta sama strona przeczytana przez Google Vision `DOCUMENT_TEXT_DETECTION` i złożona NASZĄ siatką — trzeci, zdekorelowany głos; `src/ocr-google.js` uwierzytelnia się istniejącym `GOOGLE_SERVICE_ACCOUNT_JSON` od Arkuszy, `src/ocr-tabela.js` przypisuje słowa do rubryk; darmowe do 1000 stron/mies.; wymaga włączenia Vision API w projekcie GCP). Auth `x-token` = `PREPROCESS_TOKEN`. Silnik: 4 obrazy na kartę + jedno wywołanie opus-5 (structured outputs, polityka zapis/wniosek) + ślepy odczyt nazwiska + ślepa kolumna RAZEM; godziny wejścia/wyjścia jako sufit (stajnia liczy z RAZEM, reszta z godzin); urlop/chorobowe wg długości zmiany, a przy 12/12 wg **grafiku** (`src/grafik.js` parsuje arkusze działów z Google Sheets; imiona z grafiku dopasowywane po zdrobnieniach + jawna mapa `imionaGrafiku` w `czytnik/dane/pracownicy.json` dla imion niejednoznacznych); dogrywka zoomem spornych pól i `paczkaRewizyjna` z wycinkami dla człowieka. `npm run eval` przepuszcza zapisane odczyty przez walidację **bez wywołań API**. Szczegóły: `czytnik/README.md` |
| `/api/contractors` | `routes/contractors.js` | kontrahenci PL: upsert, 360, **adresy (structured-address / delivery-address)**, merge, geocode, find-address (maile/GK), **GET :id/shipment-addresses** (adresy dostaw ze WSZYSTKICH źródeł: ContractorAddress → extras.locations → wysyłki powiązane przez Transaction/Invoice → adres paczki z GK po NUMERZE — działa gdy nazwa odbiorcy ≠ nazwa kontrahenta; zbiera też z DUPLIKATÓW po NIP/nazwie) |
| `/api/deals` | `routes/deals.js` | deale |
| `/api` | `routes/sales-agents.js` | **Agenci handlowi** (zakładka Agenci): CRUD agentów (**waluta rozliczeń EUR/PLN** per agent), **cennik agenta** (`/prices` — inna cena na każdy produkt, upsert po nazwie), przypinanie kontrahentów PL/ES NA STAŁE (bez dat — FV liczą się automatycznie od PIERWSZEJ dostawy agenta albo wcześniejszej korekty salda: `agentCutoff`; karta zwraca `fvOd`), operacje delivery/settlement/adjustment (waluta domyślnie = waluta agenta; **z datą wstecz** do uzupełniania historii), PATCH przypięcia (`since` — od kiedy FV liczą się do salda, edytowalne). Karta liczy też **saldo narastająco** (`saldoAfter` przy każdej pozycji historii); saldo początkowe przy stałym obrocie = operacja `adjustment` (kwota ze znakiem) na dzień startu ewidencji. **Saldo per waluta = Σ OPŁACONEJ części FV kontrahentów agenta (paidAmount PL / totalPayedAmount ES; wystawienie FV nie rusza salda, częściowa wpłata liczy się częściowo) − towar wydany − rozliczenia + korekty**; nieopłacone kwoty wracają osobno jako `unpaidInvoiced` („w nieopłaconych FV") (np. 100×4,50 FV − 100×3 towar = 150 €); karta agenta zwraca też scaloną historię (operacje + FV) |
| `/api/consignments` | `routes/consignments.js` | komisy/konsygnacje |
| `/poczta` | `routes/poczta-zdrowie.js` | **GET /poczta/zdrowie** (`?inbox=`, `?progCiszyMin=`) — jedno zapytanie: czy skrzynki NAPRAWDĘ pobierają. Zestawia trzy źródła: `ImapState` (lastUid, uidValidity, lastOkAt), bazę (ostatni mail INBOUND, liczba w dobie/tygodniu, `EmailSkip` z ostatniej doby) i **stan serwera IMAP na żywo**. Kluczowa liczba: **`zaleglosc` = (uidNext-1) − lastUid**, czyli ile wiadomości leży na serwerze ponad tym, co pobraliśmy — zero znaczy „na bieżąco”, cokolwiek innego znaczy „poczta stoi”, niezależnie od tego, czy połączenie zgłasza błąd. Zwraca też **`ostatniaNaSerwerze`** (INTERNALDATE najnowszej wiadomości w INBOX, czytana po numerze porządkowym) — to ona odróżnia „nic nie przyszło” od „przyszło i zgubiliśmy”. Werdykt per skrzynka: `ok` / `zaleglosc` / **`gubimy`** (na serwerze leży coś nowszego niż nasz najnowszy mail — łapie przypadki, gdzie zaległość po UID = 0) / `awaria` / `cisza`. Zwraca WYŁĄCZNIE metadane (bez treści maili, nadawcy skrócone do domeny). Auth `x-token` = `PREPROCESS_TOKEN`, poza `/api` (jak `/czytnik/*`). Powstało po cichej awarii michal@ 3.09.2026: `/emails/imap-diag` pokazywał foldery, ale nie zestawiał ich z bazą, więc nie było widać, że zostajemy w tyle |
| `/api` | `routes/emails.js` | maile: lista (limit≤1000 + `offset`; filtr `openDeal=1` = niedomknięte deale), wątki, send-email, translate, read, bulk, **PATCH :id/deal** (flaga openDeal na kontrahencie / tag deal-open; FV zamyka deal — `closeOpenDealOnInvoice` w confirm), **POST /emails/scan-open-deals** (LLM-skan wstecz W TLE: kandydaci deterministycznie → WSZYSCY w partiach po 50 na call Sonnet → auto-oznaczenie; raport z oldestMailAt (realny zasięg danych); wynik w AgentContext deal-scan, odczyt **GET .../status**; `{days,apply,sync}`) |
| `/api/mailing` | `routes/mailing.js` | kampanie mailingowe |
| `/api/products` | `routes/products.js` | produkty (katalog PL) |
| `/api` | `routes/config.js` | Config (klucze konfiguracyjne w DB) |
| `/api` | `routes/invoices.js` | **FV PL (iFirma)**: invoice-preview / invoice-confirm(-latest), pay (przed wpłatą ODŚWIEŻA brutto i **Zapłacono** z iFirmy — płaci tylko RESZTĘ; FV opłacona → tylko sync statusu; **FV Z KOREKTĄ** (PelnyNumerKorekty) → wymaga jawnej kwoty po korekcie, po wpłacie wyrównuje lokalne brutto), ksef-status (numer KSeF czyta z IFIRMY — `ifirma-ksef-sync.pullKsefNumberFromIfirma`, bez limitu 20/h; kanał KSeF tylko jako zapas przy błędzie iFirmy), **ksef-pending** (które FV nie mają numeru KSeF = brak ✅; TYLKO z bazy, zero zapytań do KSeF, `?months=N`), **ksef-probe?number=190/2026** i **:id/ifirma-ksef-probe** (diagnostyka: czy iFirma oddaje numer KSeF w szczegółach FV — alternatywa bez limitu 20/h; wariant z `?number=` bo numery FV mają ukośnik i nie mieszczą się w ścieżce), shipment-doc, last-price (zwraca cenę+walutę+typ z ostatniej FV; preview przyjmuje override `currency`/`rodzaj` → dowolna forma FV, w tym **WDT w złotówkach** — `ifirma-client.createInvoice` przy WDT-PLN idzie na fakturawdt.json z Walutą PLN, konto PLN, VAT 0% NP), link/unlink-shipment, **invoice-draft-from-email** (prefill formularza FV z maila; pozycje mapowane na NASZ katalog jak parse-order — bez koloru → generic „Dowolny"; baza: kontrahent z tabeli PL → ZAWSZE iFirma (Hiszpania kontynentalna = WDT), Contasimple tylko gdy klient jest w EsContractor), **parse-order** (rozbij wklejone zamówienie na pozycje+ceny, Sonnet), **fix-unknown-numbers** (nadrób numery FV zapisane jako UNKNOWN), **sync-history** (pełny import historii z iFirmy W TLE: miesiąc po miesiącu od fromDate, kontrahenci po NIP + FV po ifirmaId idempotentnie, bez kasowania; postęp GET /ifirma/sync-history/status), **fix-brutto-as-netto** (napraw pozycje z brutto w polach netto; dryRun→confirm), **:id/payment-reminder-preview|-send** (🔪 przypomnienie o płatności: wiadomość w języku kontrahenta + PDF FV w załączniku; serwis `payment-reminder.js`; nadawca domyślnie info@ (ES w contasimple.js: nikodem@), zmienialny — preview zwraca `from`+`accounts`, send przyjmuje `from`), cache zamówień GK |
| `/api/jpk` | `routes/jpk.js` | JPK + dopasowanie WDT (`performWdtMatching`) |
| `/api/jpk` | `routes/jpk-package.js` | paczka WDT/eksport (CMR) — build/send do księgowej |
| `/api` | `routes/parse-document.js` | parsowanie dokumentów |
| `/api` | `routes/analytics.js` | analityka sprzedaży (`/revenue` zwraca też `paid` per bucket = zapłacona część obrotu: PL SUM paidAmount/status, ES totalPayedAmount/status) |
| `/api` | `routes/glob.js` → `glob-sync`, `glob-orders`, `glob-quote` | **GlobKurier**: senders, orders, **quote**, **order**, send-label, delete-order, presets, calculate-package. sync-receivers dopasowuje kontrahenta scorerem (minScore 75), nie po 1. słowie nazwy. **Quote z wklejonym adresem: wklejona nazwa/adres są nadrzędne** — kontrahent z bazy podpinany tylko przy pewnym matchu (score ≥ 80 / równość zbioru słów), nigdy nie nadpisuje nazwy odbiorcy |
| `/api` | `routes/agent.js` | **agenci AI**: /agent/{logistics,accounting,accounting-es,communication,communication-es,operations,sudo}, /agent/assistant (router Haiku; do kontekstu sub-agentów wstrzykuje NIP-y/VAT UE znalezione regexem w PEŁNEJ treści maila/wątku — `country-helper.findEuVatsInText`), /agent/email-context. **Odczyt załączników** (`extractEmailAttachments`/`extractAssistantAttachments`, używane też przez invoice-draft-from-email): PDF → pdf-parse, obrazy → vision, **Excel .xlsx → exceljs (arkusze do tekstu)**, CSV/TXT → wprost; stary .xls nieobsługiwany (komunikat) |
| `/api` | `routes/upload.js` | upload plików |
| `/api` | `routes/telegram-callback.js` | **tapnięcia guzików Telegram** (zatwierdź FV, zamów kuriera, odrzuć) |
| `/api` | `routes/ksef.js` | KSeF: `auth-test`, `sync-sales-status` (ręcznie przez API KSeF, dowolny zakres — audyt/zapas), `autosync-sales` (**czyta z IFIRMY** przez `ifirma-ksef-sync`, throttle 15 min w Config, 2 mies. wstecz), `pull-cost-invoices`, `autosync-costs` (co 6 h, TYLKO bieżący+poprzedni miesiąc — pełny rok ręcznie), `cost-invoices`. **Limit API KSeF: 20 zapytań `/invoices/query/metadata` na godzinę** — dlatego numery sprzedaży idą z iFirmy, a API KSeF zostaje do kosztów i audytu |
| `/api` | `routes/costs.js` | faktury kosztowe |
| `/api` | `routes/accounting.js` | **„Dodatkowa księgowość"**: monthly-report (dogrywa numery KSeF w tle **z iFirmy** przez throttle 10 min — raport ładuje się przy wejściu/zmianie miesiąca/po każdej akcji, bez throttla każdy z tych momentów puszczałby serię), refresh-ksef-status (guzik „Odśwież status KSeF” — wymuszony odczyt **z iFirmy**; budżet 25 s, reszta w `remaining`; `ok:false` gdy iFirma padła w całości albo inny przebieg trwa), send-month-to-ksef (masowa wysyłka + odczyt numerów z iFirmy po ~90 s, retry raz przy `busy`), pair-wdt, pair-wdt-one |
| `/api` | `routes/admin.js` | **contractor-cleanup** (edycja kontrahenta), **contractors/merge** + **contractors/dedupe-nip** (scalanie duplikatów po NIP), vies-check, **transactions/reassign-by-invoice** (przepnij transakcję do kontrahenta z FV — sprzątanie po starym fuzzy-parowaniu), **contractors/split** (ROZKLEJENIE błędnie scalonych: przenosi FV po numerach + transakcje + maile po adresie/domenie na innego/nowego kontrahenta; dryRun→confirm), **ifirma-fix-eu-prefix** (przepycha zagranicznych kontrahentów przez `upsertContractor`, żeby prefiks UE trafił do OSOBNEJ rubryki w iFirmie zamiast do NIP-u — inaczej KSeF odrzuca; dryRun→bieg w tle, postęp GET `ifirma-fix-eu-prefix-status`), backfille |
| `/api` | `routes/activity.js` | oś zdarzeń (ActivityEvent) |
| `/api` | `routes/cron.js` | zadania cykliczne (raport miesięczny, sync) |
| `/api` | `routes/transactions.js` | transakcje (deal cycle) |
| `/api` | `routes/push.js` | web push (PWA) |
| `/api/contasimple` | `routes/contasimple.js` | **FV ES/Kanary**: invoice-preview/confirm(-latest), albaran (WZ), delete, products/contractors ES, owner (PUT :id/owner + set-owner-batch), **invoices/backfill-contractor-mapping** (retro-fix kontrahentow FV z CS; realny przebieg W TLE, postep GET /backfill-contractor-status; klient z originalTargetEntityID/target — nowy format CS), **GET local-invoices** (lista FV ES z bazy; filtry `search`/`status`/`fromDate`/`toDate`/`owner`/`contasimpleOnly`/`withLines`, **`numberFrom`/`numberTo`** = zakres po numerze (porównanie ostatniej grupy cyfr, działa dla „0096" i „2026-0096"; przy zakresie limit domyślnie 10000), **`format=text`** = czytelny raport tekstowy z pozycjami, statusem płatności i podsumowaniem do zapłaty — do Konsoli API) |
| `/api` | `routes/mk.js` | **Mała Księgowość (mk.app)** — ETAP 0: `mk/ping`, `mk/ksef-fetch` (wyzwól pobranie z KSeF: buy/sell), `mk/ksef-fetch/:ref` (status), `mk/cost-invoices`, `mk/sales-invoices`, `mk/new-ledger-entries`. Reconciliation MK↔baza↔iFirma↔KSeF = ETAP 2 |

---

## 3. Klienci zewnętrznych API (`src/*.js`)

- `ifirma-client.js` — iFirma (HMAC). `createInvoice` (WDT też w PLN), `upsertContractor` (**rozdziela prefiks UE od NIP → PrefiksUE + NIP osobno**, inaczej KSeF odrzuca zagraniczne; `country-helper.splitEuVat`). **`createInvoice` robi to samo** dla bloku `Kontrahent` (`NIP`+`PrefiksUE`) i pól `NIPKontrahenta`/`PrefiksUEKontrahenta` — wcześniej pchał cały „IT02061460222" w NIP i iFirma tak zapisywała kontrahenta. Prefiks bierzemy z NUMERU VAT, nie z kraju (Grecja: kraj GR, VAT EL), `registerPayment` (Opłacono), pobranie listy FV i PDF.
- `contasimple-client.js` — Contasimple (ES): faktury, WZ, formaty numeracji.
- `glob-client.js` — GlobKurier: getQuote, getOrders, createOrder, labels, receivers, countries, pickupTimeRanges.
- `ksef-client.js` — KSeF (token RO): pobieranie faktur sprzedaż (Subject1) / koszty (Subject2). **`getAccessToken()`** — token z cache 5 min (pełny handshake to ~10 żądań). **Cooldown po 429**: limit 20 zapytań metadata/h zapamiętywany globalnie (`metadataCooldownMs()`), w trakcie karencji klient NIE rusza sieci, tylko od razu rzuca błąd z czasem oczekiwania. **Zakres dat > 3 mies.** (KSeF błąd 21405) dzielony automatycznie na okna po ≤85 dni — każde okno to osobne żądanie, więc powyżej 12 okien klient odmawia zamiast przepalić limit.
- `mk-client.js` — **Mała Księgowość (mk.app)**: auth (X-API-Key / login→JWT / data-sharing-key, env `MK_*`), wyzwolenie pobrania z KSeF (`ksefFetch` buy/sell), odczyt ledger (vat-purchase/vat-sales/new-ledger-entries/invoices).
- `mail-sender.js` — SMTP (wysyłka maili); centralnie dokleja stopkę nadawcy z `services/signatures.js` (marker chroni przed dublem).
- `vies.js` — walidacja VAT UE (VIES).
- `telegram-utils.js` — `sendTelegram` / `sendTelegramDocument` / `sendTelegramPhoto`, `answerCallbackQuery`, `editMessageReplyMarkup`.
- `http.js` — `fetchWithTimeout`. `asyncHandler.js` — opakowanie błędów Express. `db.js` — klient Prisma.

### Odbiór maili
- `inbox-poller.js` — **FILTR MOŻE UKRYĆ, NIE MOŻE SKASOWAĆ** (3.09.2026): ocena modelu (`SPAM`/`AUTO_REPLY`) NIE usuwa już maila — mail jest zapisywany z tagiem `ukryty-filtrem` i `isRead=true` (nie idzie na Telegram, ale JEST i da się go odzyskać). `EmailSkip` zostaje tylko dla filtrów deterministycznych (bounce, newsletter z `List-Unsubscribe`); stare wpisy `ai:*` NIE blokują już rescanu i są przy nim kasowane. `czyZnanyNadawca` (pisaliśmy do niego / jest w `ContractorContact` / domena własna lub z `ZAUFANE_DOMENY`) całkiem zdejmuje ocenę modelu — relacja z bazy bije ocenę treści. **`czuwajNadCisza`** co godzinę: (a) „filtr coś wyrzuca, a nic nie zapisujemy” — dowód utraty, alarm zawsze; (b) cisza — próg **kalibrowany per skrzynka**: mediana odstępów z ostatnich 20 maili × 3, nie mniej niż 48 h. Płaski próg („doba ciszy przy ≥5 mailach/tydzień”) od razu dał fałszywy alarm na info@, która dostaje ~1 mail dziennie — alert, który wyje bez powodu, przestaje być czytany i wracamy do awarii, której nikt nie zauważa. Telegram 1×/dobę na skrzynkę. Powód: michal@ przez 20 dni nie miał w CRM ani jednego maila — połączenie działało, klasyfikator kasował, alert milczał, bo pilnował tylko błędów połączenia.
- `inbox-poller.js` — IMAP co 5 min: filtry (spam/bounce/newsletter), klasyfikacja AI (Haiku), zapis Email + załączniki, **powiadomienie Telegram**; ścieżki specjalne: **web-order** (`isWebOrder`) i **attachment-order** (PDF → `order-llm-parser.js` → „📋 ZAMÓWIENIE Z ZAŁĄCZNIKA"). Decyzje odfiltrowania trwałe w `EmailSkip` (rescan stosuje te same filtry + EmailSkip — nie wpuszcza spamu z powrotem). **UIDVALIDITY** w `ImapState` — reset skrzynki wykrywany, lastUid przestawiany na nową numerację + catch-up rescanem. Health-alert skrzynki.
- `imap-sent.js` — skan folderu Wysłane.
- `order-llm-parser.js` — LLM parser zamówień z tekstu PDF (model `ORDER_PARSER_MODEL`). Dostaje NASZ katalog (`getActiveCatalog`) i mapuje pozycje na nasze produkty (zwraca nasz ean+nazwę, nie łączy dwóch produktów w jedną pozycję). `parse-order` dodatkowo domyka każdą pozycję przez `findProductFuzzy` (koszyk = nasze nazwy+EAN jak z guzików).

### Stany podglądów (in-memory, TTL 30 min)
- `stores.js` — podglądy FV PL (`invoicePreviews`). `es-stores.js` — podglądy FV/WZ ES.
  Trwała idempotencja wystawiania: **`services/confirm-lock.js`** (model `ConfirmLock`).

---

## 4. Serwisy (`src/services/`)

### Agenci AI
- `agent-loop-base.js` — pętla narzędzi, prompt caching, obsługa overloaded (`OVERLOAD_TEXT`).
- `agent-runtime.js` — `buildExecuteTool`, `selfCall` (wewn. HTTP), sanitizacja.
- `logistics-agent.js` — kurier (quote/order/track/label/delete, szukanie adresu). Model `LOGISTICS_AGENT_MODEL`.
- `accounting-agent.js` — FV PL, NIP/VIES, **upsert_contractor** (rozbija adres na pola; nazwa prawna > handlowa), **extract_nip** (skan pełnych treści maili nadawcy zanim powie „brak VAT").
- `accounting-agent-es.js` — FV ES/Kanary.
- `communication-agent.js` / `communication-agent-es.js` — maile/odpowiedzi/tłumaczenia.
- `operations-agent.js` — deale, transakcje, matching FV↔wysyłka, Google Sheets.
- `sudo-agent.js` — administracyjny (call_endpoint).

### Faktury / księgowość
- `ifirma-payload.js` — **`buildIfirmaContractorPayload`**: składa Kontrahenta do iFirmy; postCode z kolumny → ContractorAddress(billing) → extras → regex → duplikat po NIP → **adres DOSTAWY/extras.locations (kod z delivery, gdy billing bez kodu)** → lista kontrahentów iFirmy → **detal ostatniej FV w iFirmie** (pełny blok Kontrahent — dla kontrahentów z importu historii); odzyskany adres UTRWALANY na kontrahencie. Kraj: gdy pusty a kontrahent zagraniczny — dobiera z prefiksu NIP UE / formy prawnej (ApS→DK). Na FV **krajowej** dla zagranicznego klienta iFirma dostaje `Kraj`=polska nazwa (`country-helper.toIfirmaKraj`), inaczej odrzuca kod pocztowy jako polski.
- `ifirma-sync.js`, `ifirma-pdf-parser.js` — sync i parsowanie PDF iFirma.
- `monthly-accounting.js` — zakres miesiąca + `buildReport` (pokrycie KSeF + WDT sparowane/niesparowane).
- `ifirma-ksef-sync.js` — **główny kanał numerów KSeF sprzedaży**: iFirma zwraca `NumerKSEF` w szczegółach FV (odkryte sondą ksef-probe), bez limitu 20/h i z widocznością wysyłek zrobionych ręcznie w panelu iFirmy. `pullKsefNumberFromIfirma` (jedna FV → zapis `Invoice.ksefNumber`), `syncMissingFromIfirma({from,to,limit,budgetMs})` (FV bez numeru w zakresie; sekwencyjnie z throttlem 400 ms; `remaining` gdy budżet się skończy, `errorCount/errors` gdy iFirma sypie; **globalna blokada `running`** — przebiegi się nie nakładają; **data-bez-godziny w `to` rozciągana do końca dnia** — inaczej FV z ostatniego dnia miesiąca wypadały; **proformy wykluczone** (KSeF ich nie przyjmuje — wisiałyby jako wieczni kandydaci); pamięć `lastChecked` rotuje kandydatów, żeby ogon nie głodował, i deduplikuje polling ksef-status) i `syncMissingFromIfirmaThrottled` (throttle w Config pod WŁASNYM kluczem `autosync:ksef:ifirmaSales` + atomowy check-and-set — celowo innym niż klucz kanału KSeF, to niezależne limity). **Budżety inline ≤ 25 s**, bo budżet sprawdzany PRZED wywołaniem, a jedno wywołanie iFirmy może trwać 30 s (timeout) — razem musi się mieścić w 60 s proxy. Używane przez ksef-status, accounting (guzik/raport/po masowej wysyłce) i autosync-sales.
- `ksef-sales-sync.js` — odczyt statusu przez API KSeF (Subject1) — dziś ZAPAS/audyt: `syncSalesStatus` (ręczny sync-sales-status) i `syncSalesStatusThrottled` (fallback ksef-status gdy iFirma nie odpowiada; wspólny throttle w `Config['autosync:ksef:salesStatus']` + poszanowanie cooldownu 429).
- `wdt-pairing.js` — **`pairWdtSmart`** (Opus): dopasowanie FV WDT↔wysyłki + weryfikacja kraju (`isToPoland`); **`suggestForInvoice`** — podpowiedzi LLM dla DOWOLNEJ faktury (parowanie „LLM" przy fakturze, bez reguły zagranicy).
- `bin-check.js` — **realny test binariów systemowych** (`pdfinfo`, `pdftoppm`, `convert`, `tesseract`, `gs`): odpala proces i patrzy, czy wstał — świadomie NIE czyta configu buildu, bo ten potrafi być ignorowany i kłamać. `ostrzezOBrakachPrzyStarcie()` krzyczy w logu deployu (braki nie mają czekać na pierwsze żądanie użytkownika), `sprawdzBinaria()` zasila pole `binaria` w GET /api/_version.
- `confirm-lock.js` — atomowa blokada duplikatów wystawiania (DB).
- `payment-reminder.js` — 🔪 windykacja: `composeReminder` (szablony pl/en/de/fr/es/it/nl/pt) + `resolveReminderFrom` (wybór skrzynki nadawczej: żądanie z UI > preferowany prefiks (PL info, ES nikodem) > pierwsze konto).
- `signatures.js` — stopki per nadawca (nikodem@/niko@ → podpis Nikodema); doklejane centralnie w `mail-sender.sendMail`, `SIGNATURE_MARKER` chroni przed dublem.
- `pending-preview.js` — **`invoicePreviewAlreadyIssued`**: guard dla „oczekującego podglądu FV" (`AgentContext.ksiegowosc.lastAction='preview'`). Krzyżowo sprawdza `Invoice` czy FV dla kontrahenta już powstała — chroni przed „potwierdź FV", gdy właśnie ją wystawiono (race zapisów / confirm bez zapisu 'confirmed'). Używany w `config.js` (pending-check) i `agent.js` (resolve-confirmation).
- `contasimple-helpers.js` — pomocnicze ES.
- `invoice-backfill.js`, `invoice-lines-backfill.js`, `invoice-lines-from-ifirma-backfill.js`, `invoice-snapshot-backfill.js`, `es-invoices-backfill.js` — migracje/uzupełnianie pozycji FV.

### Kontrahenci / CRM
- `contractor-match.js` — `findBestContractors`, `sameContractorName` (fuzzy).
- `contractor-merge.js` — `mergeContractors(keep,drop)` (przepina FK/kontakty/adresy/aliasy, kasuje drop) + `normalizeNipKey`. Używane przez admin/merge, admin/dedupe-nip i **auto-merge w contractors/upsert** (jeden NIP = jeden rekord).
- `contractor-sync-helpers.js`, `contractor-contacts-backfill.js`, `contractor-v2-backfill.js` — normalizacja CRM v2.
- `match-gk-order-to-contractor.js`, `find-address-in-gk-orders.js`, `address-from-emails.js` — szukanie adresu dostawy.
- `dedup-locations.js`, `country-helper.js`, `owner-derive.js`.
- `geocode.js`, `llm-geocode.js` — geokodowanie adresów.

### Logistyka / wysyłki
- `tracking-notify.js`, `tracking-urls.js` — powiadomienia trackingu do klienta.
- `label-tracking.js` — **`extractTrackingFromLabel(hash)`**: numer kuriera z PDF etykiety GK (gdy API GK jeszcze go nie zwraca, a list już jest). Używane w `glob-quote` (post-order draft) i `emails.js send-tracking-email` (fallback przed „no carrier tracking number").
- `auto-pair-shipments.js` — **auto-parowanie FV↔wysyłka po TOŻSAMOŚCI kontrahenta** (dokładna nazwa / zapisany adres kod+miasto) + data ±7 dni; zapisuje jawny link (fire-and-forget z GET /invoices i /glob/orders).
- `tracking-status.js` — **`latestTrackingEvents`**: ostatnie zdarzenie trackingu GK (`latestStatus`: „W trakcie dostarczania", „Gotowa do odbioru w punkcie" + data/lokalizacja) dla paczek w drodze; cache w pamięci TTL 20 min, kilka inline + reszta w tle. Używane w `/glob/orders` → pole `lastEvent`.
- `delivered-backfill.js` — **`backfillDeliveredDates`**: dla wysyłek DELIVERED bez `Transaction.deliveredAt` dociąga datę zdarzenia `DELIVERED_TO_RECEIVER` z historii GK `/order/tracking` i zapisuje do Transaction (update po shipmentNumber albo minimalny wpis source='delivered-backfill'). Kilka inline + reszta w tle z throttlem. Używane w `/glob/orders` i `GET /invoices` — badge „DELIVERED · X dni temu" także dla starych paczek.
- `transaction-tracker.js` — `trackInvoice`/`trackShipment` (deal cycle).
- `shipping-backfill-from-gk.js`, `match-shipments-by-query.js`.

### Infrastruktura
- `activity-log.js` / `activity-backfill.js` / `activity-prune.js` — oś zdarzeń.
- `telegram-helper.js` — `resolveTelegram` (token+chatId per scope pl/kanary).
- `product-catalog.js`, `sheets-sync.js`, `notify-mail-result.js`.
- `email-translate.js` — tłumacz maili (Haiku): `translateToPl` / `translateFromPl` (composer). Prompt = czysty silnik tłumaczeń: tłumaczy KAŻDY tekst 1:1 z zachowaniem tonu autora, NIGDY nie komentuje/poucza/dopytuje.
- `utils/address.js` — `extractPostCode`, `extractCityAfterPostCode`. `utils/email-domain.js` — `companyDomain`.

---

## 5. Modele danych (`prisma/schema.prisma`)

- **Kontrahenci:** `Contractor` (PL, flat + `extras`, **`postCode`**), `ContractorContact` (multi email/tel), `ContractorAddress` (strukturalne adresy: `billing`/`delivery`, ulica/numer/**apartment**/postalCode/miasto), `EsContractor` (ES).
- **Faktury:** `Invoice` + `InvoiceLineItem` (PL), `EsInvoice` + `EsInvoiceLineItem` (ES), `KsefCostInvoice`, `CostInvoice` (koszty manualne/upload).
- **Sprzedaż/logistyka:** `Deal`, `Transaction`, `Consignment` + `ConsignmentItem` + `ConsignmentReturn`, `Sender`, `Quote` (trwałe wyceny GK), `MonthlyPackage` (paczka WDT), `Document` (CMR/PDF).
- **Agenci handlowi:** `SalesAgent`, `SalesAgentContractor` (przypięcie kontrahenta PL/ES do agenta na stałe; jeden kontrahent → jeden agent; pole `since` już nieużywane — granicę FV wyznacza pierwsza dostawa), `SalesAgentPrice` (cennik agenta: nazwa produktu + jego cena, unikalna per agent), `SalesAgentOperation` (`delivery` towar wydany po cenie agenta / `settlement` rozliczenie np. gotówką / `adjustment` korekta ze znakiem).
- **Maile:** `Email`, `EmailAttachment`, `MailingContact`.
- **Produkty:** `Product` (PL), `EsProduct` (ES).
- **Infra:** `Config`, `Memory`, `ImapState`, **`EmailSkip`** (decyzje pominięcia maili: newsletter/AI-SPAM/AUTO_REPLY — rescan ich nie cofa), `AgentContext` (ostatni preview/draft per agent), `Activity`/`ActivityEvent`, `SystemEvent`, `AuditLog`, **`ConfirmLock`** (idempotencja wystawiania).

---

## 6. Kluczowe przepływy

- **Wystawienie FV (PL):** preview → `stores.js` + `AgentContext` → guzik/„tak" → `invoice-confirm(-latest)` → `confirm-lock` (atomowa blokada duplikatów, między instancjami) → `ifirma-client.createInvoice` → zapis `Invoice` → PDF na Telegram. ES: analogicznie przez `contasimple.js` + `es-stores.js`.
- **Wycena/zamówienie kuriera:** `/glob/quote` (z `chatId` → backend SAM pcha 1 wiadomość z guzikami; `telegramPushed`) → tap guzika → `telegram-callback.js` → `/glob/order` (dedup po quoteId) → CMR + tracking. Agent przy `telegramPushed=true` milczy (`suppressReply`). Dane odbiorcy w `/glob/order` mają fallback z poprzednich wysyłek (`contractor.extras.locations[]`, najświeższa) dla street/postCode/city/email; **telefon** normalizowany do E.164 wg kraju (`normalizePhoneE164`) i walidowany (`isPlausibleE164` — właściwy kierunkowy) — nieprawidłowy numer (np. `+688734123` dla DE) jest odrzucany i brany kolejny kandydat, ostatecznie nasz PL.
- **Lista maili (GET /api/emails):** JAWNY `select` — BEZ `bodyFull`/`bodyHtml` i bez pełnego wiersza kontrahenta (`include` bez `select` wysyłał pełny HTML każdego maila × do 1000 rekordów — główny zjadacz transferu Vercela). Treści tylko przez `GET /emails/:id`. Flaga dealu liczona jako `contractorOpenDeal` jednym zapytaniem po id.
- **Mail → Telegram:** `inbox-poller` → klasyfikacja → web-order/attachment-order/standard → Telegram + web push. **Samoleczenie po przerwie:** każda skrzynka pamięta czas ostatniego udanego cyklu (`ImapState.lastOkAt`); dziura >20 min (timeout/restart/crash) → następny udany cykl automatycznie rescanuje po dacie na głębokość przerwy (+1 dzień, max 30) i raportuje 🩹 na Telegram, gdy coś dociągnie. Skan wysłanych czyta **WSZYSTKIE** foldery „wysłanych" konta (`findSentFolders`: flagowany \Sent + Sent/Sent Messages/Wysłane…) — kopie z Thunderbirda lądują w innym folderze niż flagowany; stan UID per folder (`<inbox>:sent` + `<inbox>:sent:<folder>`). Historia na żądanie: POST /emails/sent-rescan (też wszystkie foldery). Powiadomienie o zamówieniu zapisuje pozycje+kontrahenta do `AgentContext 'order-notification'` — odpowiedź „wystaw fv na to zamówienie" dostaje je wstrzyknięte w `accounting-agent` (push żyje poza rozmową agenta).
- **WDT dla księgowej:** `accounting.js` pair-wdt(/-one) → `wdt-pairing.pairWdtSmart` (Opus + weryfikacja kraju) → `jpk-package.js` build/send CMR.
- **Adres kontrahenta → iFirma:** UI/agent → `contractors.js upsert` / `structured-address` lub `admin.js contractor-cleanup` → kolumna `postCode` + `ContractorAddress(billing)` + `extras.billingAddress` → `ifirma-payload.js` czyta przy FV.

---

## 7. Konwencje

- Modele/pola Prisma wchodzą przez `prisma db push` na starcie — nie trzeba migracji ręcznych.
- Sekrety/konfiguracja: env (Railway) + tabela `Config`.
- Modele Anthropic przez env (`LOGISTICS_AGENT_MODEL`, `ORDER_PARSER_MODEL`, `WDT_MATCH_MODEL`, …); działające: `claude-opus-4-8`, `claude-sonnet-4-5-20250929`, `claude-haiku-4-5-20251001`.
- Idempotencja wystawiania FV/WZ: zawsze przez `services/confirm-lock.js`.
- Powiadomienia Telegram per scope: `services/telegram-helper.resolveTelegram` (`pl` / `kanary`).
